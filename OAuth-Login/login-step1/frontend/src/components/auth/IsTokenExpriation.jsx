import React from 'react'

const IsTokenExpriation = (token) => {
  // 토큰 상태값을 관리하기
  // 토큰 만료 1분전에 사용자가 응답이 없으면 로그인을 연장하겠습니까???
  // 사용자가 연장해주세요! -> refresh token 을 이용하여 -> access token을 연장한다.
  
  return (
    <>
      여기서는 화면의 역할이 필요하지 않다...
    </>
  )
}

export default IsTokenExpriation
